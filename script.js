function gerarRelatorio() {
    var textoPadrao = document.getElementById("textoPadrao").value;
    var relatorio = "";

    // Lógica para gerar o relatório (pode ser similar ao exemplo anterior)

    document.getElementById("relatorio").innerText = relatorio;
}
